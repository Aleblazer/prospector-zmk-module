name: prospector-zmk-module
build:
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
  depends:
    - lvgl
    # Provides the newvision,nv3007 display driver used by the shield overlays
    - zmk-nv3007-display